"""FastAPI dependency injection system.

This module provides FastAPI dependency injection that adapts the
existing dependency injection patterns to work with FastAPI's
dependency system. It allows sharing the same business logic
containers between Lambda and FastAPI runtimes.
"""
